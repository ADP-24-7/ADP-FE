services:
  adp-fe:
    build:
      context: .
    container_name: adp-fe-console
    environment:
      VITE_APP_ENV: local
      VITE_API_MODE: real
      VITE_API_BASE_URL: http://host.docker.internal:8080
    ports:
      - "4173:4173"
